{
  "name": "github-preview",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --ip 127.0.0.1",
    "check": "node --check src/worker.js",
    "test": "node --test",
    "build": "wrangler deploy --dry-run --outdir .artifacts/build",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.130.0"
  },
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@11.23.0"
}
